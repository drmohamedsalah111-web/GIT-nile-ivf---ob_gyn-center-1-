# 🔐 دليل الوصول إلى لوحة الأدمن
# Super Admin Dashboard Access Guide

## 📍 كيفية الوصول للوحة الأدمن

### الطريقة 1️⃣: الزر الكبير في القائمة الجانبية (موصى به)

بعد تسجيل الدخول كأدمن، ستجد في أسفل القائمة الجانبية (Sidebar):

```
🔐 لوحة الأدمن
Admin Dashboard
```

**زر كبير بلون أزرق-بنفسجي - اضغط عليه مباشرة!**

---

### الطريقة 2️⃣: تحويل حساب إلى أدمن

إذا لم تكن أدمن بعد، قم بتنفيذ هذا الأمر في Supabase:

```sql
-- استبدل البريد الإلكتروني بحسابك
UPDATE doctors 
SET user_role = 'admin' 
WHERE email = 'your-email@example.com';
```

أو لجعل أول دكتور أدمن:

```sql
UPDATE doctors 
SET user_role = 'admin' 
WHERE user_role = 'doctor' 
ORDER BY created_at 
LIMIT 1;
```

ثم:
1. سجل الخروج
2. سجل الدخول مرة أخرى
3. ستظهر لك زر لوحة الأدمن

---

## 🎯 ماذا ستجد في لوحة الأدمن؟

### 1. إدارة الاشتراكات (Subscription Management)
- عرض جميع اشتراكات العيادات
- تجديد وتعديل الاشتراكات
- إحصائيات مفصلة
- تنبيهات الاشتراكات المنتهية

### 2. إدارة المستخدمين (قريباً)
- عرض جميع المستخدمين
- إضافة وتعديل الصلاحيات

### 3. التقارير والإحصائيات (قريباً)
- إحصائيات شاملة للنظام
- تقارير الإيرادات

### 4. إعدادات النظام (قريباً)
- إعدادات عامة
- صيانة قاعدة البيانات

---

## ✅ التحقق من الصلاحيات

لمعرفة من هو الأدمن:

```sql
SELECT email, user_role, name 
FROM doctors 
WHERE user_role = 'admin';
```

---

## 🎨 مميزات لوحة الأدمن الجديدة

✅ تصميم حديث وجذاب  
✅ زر كبير وواضح للوصول السريع  
✅ بطاقات تفاعلية لكل قسم  
✅ إحصائيات سريعة في الأعلى  
✅ واجهة عربية/إنجليزية  
✅ محمية بصلاحيات Admin فقط  

---

## 🔒 الأمان

- الصفحة محمية بـ `RequireRole(['admin'])`
- الزر يظهر فقط إذا كان `userRole === 'admin'`
- لا يمكن الوصول للصفحة بدون صلاحيات أدمن

---

## 📱 التوافق

- ✅ Desktop (أفضل تجربة)
- ✅ Tablet
- ✅ Mobile (متوافق)

---

## 🚀 ابدأ الآن!

1. تأكد من أنك أدمن (نفذ SQL أعلاه)
2. سجل الدخول
3. ابحث عن الزر الكبير أسفل القائمة
4. اضغط عليه!

🎉 **مبروك! أصبحت الآن في لوحة تحكم الأدمن!**
