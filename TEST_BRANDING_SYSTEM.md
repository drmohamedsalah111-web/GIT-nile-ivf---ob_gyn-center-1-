# اختبار نظام الهوية البصرية الخاصة بالأطباء

## 🧪 خطوات الاختبار

### 1. إعداد قاعدة البيانات
```bash
# 1. قم بتشغيل DOCTOR_BRANDING_SIMPLE.sql في Supabase SQL Editor
# 2. تحقق من وجود الأعمدة الجديدة في جدول doctors
SELECT column_name, data_type 
FROM information_schema.columns 
WHERE table_name = 'doctors' 
AND column_name LIKE '%color%' OR column_name LIKE '%font%';
```

### 2. إنشاء Storage Buckets
```bash
# في Supabase Dashboard → Storage
# 1. أنشئ bucket باسم "doctor-logos"
# 2. اجعله public bucket
# 3. طبق سياسات RLS من DOCTOR_BRANDING_SETUP.sql
```

### 3. اختبار الواجهة الأمامية
```bash
# 1. سجل الدخول كطبيب
# 2. اذهب إلى صفحة الإعدادات
# 3. اختر تبويب "المظهر والهوية"
# 4. قم بتغيير الألوان والخطوط
# 5. ارفع شعار جديد
# 6. احفظ الإعدادات
```

## 🧪 قائمة التحقق

### ✅ الاختبارات الأساسية
- [ ] يتم عرض صفحة الإعدادات بدون أخطاء
- [ ] تظهر جميع خيارات الألوان
- [ ] تعمل قوائم الخطوط والأنماط
- [ ] يتم رفع الشعار بنجاح
- [ ] تظهر رسالة نجاح عند الحفظ

### ✅ اختبار البيانات
- [ ] يتم حفظ البيانات في جدول doctors
- [ ] يتم رفع الشعار إلى bucket الصحيح
- [ ] تتم مزامنة البيانات مع Supabase
- [ ] تستمر البيانات بعد تحديث الصفحة

### ✅ اختبار الشريط الجانبي
- [ ] يعرض شعار الطبيب المخصص
- [ ] يستخدم ألوان الطبيب المخصصة
- [ ] يظهر اسم العيادة الصحيح
- [ ] تأخذ الأزرار شكل الطبيب المخصص

### ✅ اختبار الأداء
- [ ] تحميل سريع للصفحة
- [ ] لا يوجد تأخير في حفظ البيانات
- [ ] يعمل بشكل صحيح في وضع Offline
- [ ] يتم مزامنة التغييرات فوراً

## 🔧 أدوات التصحيح

### إذا لم يعمل حفظ البيانات:
```javascript
// تحقق من console للمشاكل
console.log('Branding update error:', error);

// تحقق من وجود المستخدم
const user = await authService.getCurrentUser();
console.log('Current user:', user);

// تحقق من صلاحيات RLS
const results = await powerSyncDb.getAll('SELECT * FROM doctors WHERE user_id = ?', [user.id]);
console.log('Doctor data:', results);
```

### إذا لم يرفع الشعار:
```javascript
// تحقق من وجود bucket
const { data, error } = await supabase.storage.getBucket('doctor-logos');
console.log('Bucket exists:', data, error);

// تحقق من سياسات RLS
const { data: files } = await supabase.storage
  .from('doctor-logos')
  .list();
console.log('Accessible files:', files);
```

### إذا لم تظهر الألوان في الشريط الجانبي:
```javascript
// تحقق من بيانات الهوية البصرية
const { branding } = useBranding();
console.log('Branding data:', branding);

// تحقق من تطبيق الأنماط
const buttonStyle = branding?.button_style || 'rounded';
console.log('Button style:', buttonStyle);
```

## 📱 اختبار على المتصفحات المختلفة

### Chrome/Firefox
- [ ] تعمل جميع الألوان
- [ ] تظهر الخطوط بشكل صحيح
- [ ] تعمل الأنماط المخصصة

### Safari
- [ ] التوافق مع line-height
- [ ] عرض الخطوط العربية بشكل صحيح

### Mobile
- [ ] تعمل الألوان على الشاشات الصغيرة
- [ ] تتناسب الأزرار مع الشاشات اللمسية
- [ ] لا يوجد مشاكل في التمرير

## 🚀 الإطلاق النهائي

بعد إتمام جميع الاختبارات بنجاح:

1. ✅ قم بتحديث `IMPLEMENTATION_SUMMARY.md` بالنتائج
2. ✅ أضف ملاحظات حول أي مشاكل واجهتها
3. ✅ جهز دليل المستخدم النهائي
4. ✅ كن مستعداً لدعم المستخدمين

## 📞 المشاكل الشائعة والحلول

### المشكلة: "فشل تحديث بيانات الهوية البصرية"
**الحل**: تحقق من:
- صلاحيات RLS على جدول doctors
- وجود أعمدة الهوية البصرية
- صلاحيات المستخدم في PowerSync

### المشكلة: لا يظهر الشعار
**الحل**: تحقق من:
- وجود bucket "doctor-logos"
- سياسات RLS على storage
- رابط الصور العام

### المشكلة: الألوان لا تتغير
**الحل**: تحقق من:
- تحديث state بشكل صحيح في React
- تطبيق الأنماط في CSS
- عدم وجود أخطاء في JavaScript

---
**ملاحظة**: قم بتشغيل هذه الاختبارات خطوة بخطوة وتسجيل النتائج للتأكد من أن كل شيء يعمل بشكل مثالي قبل الإطلاق النهائي للنظام.
