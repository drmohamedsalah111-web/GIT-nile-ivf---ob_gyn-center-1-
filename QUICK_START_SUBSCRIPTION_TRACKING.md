# 🚀 دليل التنفيذ السريع - نظام تتبع الأيام المتبقية
# QUICK START GUIDE - Subscription Days Tracking System

---

## ⚡ التنفيذ في 5 دقائق

### 1️⃣ تنفيذ SQL (دقيقتان)

```bash
# افتح Supabase Dashboard
# اذهب إلى SQL Editor
# انسخ والصق محتوى الملف التالي:
```

**الملف:** `SUBSCRIPTION_DAYS_TRACKING_SYSTEM.sql`

```sql
-- نفذ هذا الملف بالكامل في SQL Editor
-- سيقوم بـ:
-- ✅ إنشاء جميع الدوال
-- ✅ إنشاء جميع الـ Views
-- ✅ إنشاء جدول التنبيهات
-- ✅ تشغيل استعلامات التحقق
```

### 2️⃣ التحقق من التثبيت (30 ثانية)

```sql
-- استعلام سريع للتحقق
SELECT 
  clinic_name,
  plan_name_ar,
  days_remaining,
  expiry_status_ar
FROM subscription_tracking_details
LIMIT 5;
```

إذا عملت دون أخطاء، التثبيت ناجح! ✅

### 3️⃣ فتح الواجهة (10 ثوان)

```
1. افتح لوحة السوبر ادمن
2. من القائمة الجانبية، ستجد تبويب جديد: 
   📅 تتبع الأيام المتبقية
3. اضغط عليه
4. استمتع!
```

---

## 📋 الملفات المُنشأة

| الملف | الوصف | الموقع |
|------|-------|--------|
| `SUBSCRIPTION_DAYS_TRACKING_SYSTEM.sql` | دوال وجداول SQL | قاعدة البيانات |
| `SubscriptionExpiryTracker.tsx` | واجهة React | `src/pages/admin/` |
| `SUBSCRIPTION_DAYS_TRACKING_GUIDE.md` | دليل شامل | جذر المشروع |
| `QUICK_START.md` | هذا الملف | جذر المشروع |

---

## 🎯 الميزات الرئيسية

### ✅ ما تم إنشاؤه

#### في قاعدة البيانات
- ✅ دالة `get_days_remaining()` - حساب الأيام المتبقية
- ✅ دالة `get_subscription_usage_percentage()` - نسبة الاستهلاك
- ✅ دالة `auto_expire_subscriptions()` - تحديث تلقائي
- ✅ دالة `get_clinic_subscription_summary()` - ملخص عيادة
- ✅ دالة `get_subscriptions_by_days_range()` - بحث بنطاق
- ✅ View `subscription_tracking_details` - عرض شامل
- ✅ View `active_subscriptions_expiring_soon` - قريبة من الانتهاء
- ✅ View `critical_expiring_subscriptions` - عاجلة
- ✅ View `subscription_expiry_statistics` - إحصائيات
- ✅ جدول `subscription_expiry_notifications` - سجل التنبيهات

#### في الواجهة
- ✅ صفحة كاملة بتصميم احترافي
- ✅ بطاقات إحصائية ملونة
- ✅ فلترة متعددة (حالة، تجديد، ترتيب)
- ✅ جدول تفاعلي مع ألوان ديناميكية
- ✅ تصدير CSV
- ✅ تحديث فوري
- ✅ تكامل كامل مع لوحة السوبر ادمن

---

## 🔍 أمثلة سريعة

### مثال 1: عرض الاشتراكات العاجلة

```sql
SELECT * FROM critical_expiring_subscriptions;
```

### مثال 2: عرض الاشتراكات التي تنتهي خلال 30 يوم

```sql
SELECT * FROM active_subscriptions_expiring_soon;
```

### مثال 3: إحصائيات سريعة

```sql
SELECT * FROM subscription_expiry_statistics;
```

### مثال 4: البحث عن عيادة محددة

```sql
SELECT * FROM subscription_tracking_details
WHERE clinic_name LIKE '%اسم العيادة%';
```

### مثال 5: تحديث الاشتراكات المنتهية

```sql
SELECT auto_expire_subscriptions();
```

---

## 📊 التصنيفات والألوان

| الحالة | الأيام المتبقية | اللون | الأيقونة |
|--------|------------------|-------|----------|
| عاجل جداً | ≤ 3 | 🔴 أحمر | ⚠️ |
| عاجل | 4-7 | 🟠 برتقالي | ⚠️ |
| تحذير | 8-15 | 🟡 أصفر | 🕐 |
| انتبه | 16-30 | 🔵 أزرق | 📅 |
| طبيعي | > 30 | 🟢 أخضر | ✅ |
| منتهي اليوم | 0 | 🔴 أحمر وامض | ⚠️ |

---

## 🎨 لقطات من الواجهة

### البطاقات الإحصائية
```
┌─────────────┬─────────────┬─────────────┐
│  عاجل جداً  │    عاجل    │   تحذير    │
│     🔴      │     🟠      │     🟡      │
│      5      │      12     │      8      │
└─────────────┴─────────────┴─────────────┘
```

### الفلاتر
```
┌──────────────────┬──────────────────┬──────────────────┐
│  حالة الانتهاء   │  التجديد التلقائي│   الترتيب حسب   │
│   [القائمة ▼]    │   [القائمة ▼]    │   [القائمة ▼]    │
└──────────────────┴──────────────────┴──────────────────┘
```

### الجدول
```
┌─────────────┬────────┬────────┬─────────┬─────────┬──────────┐
│   العيادة   │ الباقة │ الحالة │ الأيام  │ الاستهلاك│ الإجراءات│
├─────────────┼────────┼────────┼─────────┼─────────┼──────────┤
│ عيادة النيل │ متقدمة │ عاجل  │   5     │  ████ 90%│  🔔 📊  │
│ عيادة مصر  │ أساسية │ تحذير │   12    │  ███░ 75%│  🔔 📊  │
└─────────────┴────────┴────────┴─────────┴─────────┴──────────┘
```

---

## 🔧 الصيانة الدورية

### يومياً (تلقائي)
```sql
-- أضف هذا كـ Cron Job يومي
SELECT auto_expire_subscriptions();
```

### أسبوعياً (يدوي)
```
1. افتح صفحة التتبع
2. راجع الإحصائيات
3. اضغط "تصدير CSV" للأرشفة
```

### شهرياً (يدوي)
```sql
-- راجع الاتجاهات الشهرية
SELECT 
  DATE_TRUNC('month', end_date) AS month,
  COUNT(*) AS expiring_count
FROM clinic_subscriptions
WHERE status IN ('active', 'trial')
  AND end_date >= CURRENT_DATE
GROUP BY month;
```

---

## 🐛 استكشاف الأخطاء

### خطأ: "relation does not exist"
**الحل:** لم يتم تنفيذ SQL بشكل صحيح
```sql
-- أعد تنفيذ الملف كاملاً
\i SUBSCRIPTION_DAYS_TRACKING_SYSTEM.sql
```

### خطأ: الصفحة لا تظهر في القائمة
**الحل:** تأكد من التحديثات في `SuperAdminDashboard.tsx`
- التحقق من الاستيراد
- التحقق من `activeTab` type
- التحقق من `menuItems`

### خطأ: البيانات فارغة
**الحل:** تحقق من وجود اشتراكات في قاعدة البيانات
```sql
SELECT COUNT(*) FROM clinic_subscriptions;
```

### خطأ: الألوان لا تظهر
**الحل:** تأكد من Tailwind CSS مُفعّل
```bash
npm run dev
```

---

## 📚 للمزيد من التفاصيل

راجع الدليل الشامل: **`SUBSCRIPTION_DAYS_TRACKING_GUIDE.md`**

يحتوي على:
- ✅ شرح مفصل لكل دالة
- ✅ 20+ تقرير SQL جاهز
- ✅ أمثلة عملية
- ✅ نصائح متقدمة
- ✅ الأسئلة الشائعة

---

## ✅ قائمة التحقق النهائية

قبل البدء، تأكد من:
- [ ] تنفيذ SQL بنجاح
- [ ] تشغيل استعلام التحقق دون أخطاء
- [ ] ظهور التبويب الجديد في لوحة السوبر ادمن
- [ ] عمل الفلاتر بشكل صحيح
- [ ] إمكانية تصدير CSV

إذا تم كل ما سبق، أنت جاهز! 🎉

---

## 🎯 الخطوات التالية

بعد التثبيت:
1. ✅ راجع البيانات في الواجهة
2. ✅ جرب الفلاتر المختلفة
3. ✅ صدّر تقرير CSV
4. ✅ اختبر الاستعلامات في SQL Editor
5. ✅ جدول تشغيل `auto_expire_subscriptions()` يومياً
6. ✅ ابدأ في بناء نظام التنبيهات (اختياري)

---

## 💡 نصيحة

احفظ هذا الاستعلام كـ Bookmark في Supabase:

```sql
-- ✨ نظرة سريعة على الاشتراكات
SELECT 
  clinic_name AS "العيادة",
  plan_name_ar AS "الباقة",
  days_remaining AS "الأيام المتبقية",
  expiry_status_ar AS "الحالة"
FROM subscription_tracking_details
WHERE status IN ('active', 'trial')
  AND days_remaining <= 30
ORDER BY days_remaining ASC;
```

---

**تم بنجاح! استمتع بالنظام 🚀**

**التاريخ:** 4 يناير 2026  
**الإصدار:** 1.0
