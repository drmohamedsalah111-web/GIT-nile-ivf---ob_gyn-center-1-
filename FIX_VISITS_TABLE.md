# 🚨 حل مشكلة "Could not find the table 'public.visits'"

## المشكلة
التطبيق يحاول حفظ بيانات الزيارة في جدول اسمه `visits`، لكن هذا الجدول غير موجود في قاعدة البيانات.

## ✅ الحل (خطوة واحدة)

### 1️⃣ شغل ملف [CREATE_VISITS_TABLE.sql](CREATE_VISITS_TABLE.sql)

1. افتح Supabase SQL Editor:
   ```
   https://app.supabase.com/project/purknrqalbkajufqfiqu/sql/new
   ```

2. انسخ محتوى ملف **`CREATE_VISITS_TABLE.sql`**

3. الصقه في SQL Editor واضغط **Run**

4. **Refresh المتصفح** (Ctrl+Shift+R)

---

## 🔍 ماذا يفعل هذا الملف؟
- ينشئ جدول `visits` المفقود
- يضيف الأعمدة المطلوبة (`diagnosis`, `prescription`, `clinical_data`, etc.)
- يضيف سياسات الأمان (RLS) للسماح للأطباء بالإضافة والتعديل

بعد هذه الخطوة، ستختفي رسالة الخطأ وسيتم حفظ الزيارات بنجاح. ✅
